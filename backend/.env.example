# Gemini API key used by app/core/contradiction.py
GEMINI_API_KEY=your_gemini_api_key_here

# Optional SQLite override for local persistence.
# If unset, the app falls back to sqlite:///./contextclock.db
CONTEXTCLOCK_DB_URL=sqlite:///./contextclock.db